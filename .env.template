STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://default:qVFOAbKmyZhOdJnaAsJIglwJEkVGsKmZ@ballast.proxy.rlwy.net:52048
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# Integration test DB admin credentials (used by @medusajs/test-utils)
# If empty, jest.config.js derives these from DATABASE_URL.
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=

# Platform OAuth + ChainUp payment provider
PLATFORM_API_URL=
PLATFORM_APP_KEY=
PLATFORM_SECRET_KEY=
PLATFORM_OAUTH_CALLBACK_URL=http://localhost:8000/auth/callback

CHAINUP_PAY_COIN_SYMBOL=USDT
CHAINUP_PAYMENT_RETURN_PAGE=http://localhost:8000
CHAINUP_PAYMENT_NOTIFY_PAGE=http://localhost:9000/hooks/payment/chainup
CHAINUP_ORDER_SCENE_TYPE=

# Optional: route outbound HTTP(S) traffic through a static-IP proxy
# Useful when ChainUp API whitelist requires a fixed egress IP.
NODE_USE_ENV_PROXY=1
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1,.railway.internal
